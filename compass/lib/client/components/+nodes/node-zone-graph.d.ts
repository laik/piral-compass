import "./node-zone-register-shape";
import './node-zone-graph.scss';
export declare class NodeZoneGraph {
    private graph;
    private width;
    private height;
    constructor(width: number, height: number);
    private initDrawGraphInstance;
}
