import { default as ChartJS } from "chart.js";
export declare const BackgroundBlock: {
    options: {};
    getOptions(chart: ChartJS): any;
    afterInit(chart: ChartJS): void;
    beforeDraw(chart: ChartJS): void;
};
