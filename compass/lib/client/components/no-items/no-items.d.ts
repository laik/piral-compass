import "./no-items.scss";
import * as React from "react";
import { IClassName } from "../../utils";
interface Props {
    className?: IClassName;
    children?: React.ReactNode;
}
export declare function NoItems(props: Props): JSX.Element;
export {};
